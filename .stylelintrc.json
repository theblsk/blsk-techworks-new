{
  "rules": {
    "at-rule-no-unknown": [
      true,
      {
        "ignoreAtRules": ["custom-variant", "theme", "utility"]
      }
    ],
    "keyframes-name-pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$"
  }
}
